{
   "type": "postgres",
   "host": "localhost",
   "port": 5432,
   "username": "svat_mvp_app",
   "password": "{{db_secret}}",
   "database": "svat_mvp",
   "synchronize": true,
   "logging": false,
   "entities": [
      "src/models/**/*.ts"
   ],
   "migrations": [
      "src/migration/**/*.ts"
   ],
   "subscribers": [
      "src/subscriber/**/*.ts"
   ],
   "cli": {
      "entitiesDir": "src/entity",
      "migrationsDir": "src/migration",
      "subscribersDir": "src/subscriber"
   }
}
